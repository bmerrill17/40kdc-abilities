name: Publish 40kdc abilities to R2

# Publishes ONLY this repo's slice as a single merged bundle (bundle-abilities.json) to
# the private 40kdc-reference bucket. The 40kdc-data repo publishes bundle-core.json
# independently. The app fetches both via presigned URLs minted by the Fly signer.
# No cross-repo coupling, no public access, no PAT.

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: publish-r2-abilities
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build bundle-abilities.json
        run: node scripts/build-abilities.mjs
        env:
          VERSION: ${{ github.sha }}

      - name: Upload bundle to R2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: auto
        run: |
          aws s3 cp dist/bundle-abilities.json "s3://${{ secrets.R2_BUCKET }}/bundle-abilities.json" \
            --endpoint-url "https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com" \
            --content-type application/json
